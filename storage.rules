rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Version: 1.0.6 - Unrestricted Access for Development
    // Ensuring global access to unblock property image uploads and views during testing.
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}